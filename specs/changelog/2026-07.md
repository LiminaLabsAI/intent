# Changelog: July 2026

- 2026-07-08: Founded Flow project (vision, principles, success criteria, roadmap) and started Phase 0 (Bootstrap).
- 2026-07-08: Completed Phase 0 (Bootstrap) — verified SQLite setup, user seeding, routing/middleware boundaries, and clean Next.js build.
- 2026-07-12: Started Phase 1 (Intent Refinement Engine) — planned chat UI, pgvector semantic search, and knowledge graph visualization.
- 2026-07-12: Released **v0.1.0** — Completed Phase 1 (Intent Refinement Engine). Added `RefinementChat` UI, `KnowledgeGraph` visualization placeholder, and SSE-based API engine with PII scrubbing and enterprise guardrails.
- 2026-07-12: Started Phase 2 (LLM Integration & Downstream Workflows) — replacing AI stubs with Vercel AI SDK and building force-directed graph UI.

- **2026-07-12** - Released v0.2.0 (Phase 3: Validation & Quality Gates) - Added LLM-powered evaluation to intercept and strictly score intents before export.
- 2026-07-14: Phase 11 (Behavior & Cost) G0 — added risk/complexity assessment types + `sized` event; readiness/requiredness now resolve by the intent's assessed risk (right-sizing).
- 2026-07-14: Phase 11 G1 — Perceive infers the obvious slots + sizes risk/complexity; DECIDE returns the full open-gap batch (no drip); agent owns termination at risk-scoped Ready.
- 2026-07-14: Phase 11 G2 — pure pre-execution cost machine (`lib/agent/cost.ts`): token estimate × per-tier price catalog → cost band, right-sized persona, refine-to-save.
- 2026-07-14: Phase 11 G3 — NARRATE voices the whole batch as one message (acknowledge captured + inferred, then all gaps); `/refine` shows a cost-advisory panel + deterministic `inferred` provenance badges.
- 2026-07-14: Phase 11 G4 — verified live on DeepSeek V4 Flash: the todo-CRUD interrogation loop is gone (actionable→ready in 2 turns, no re-asking, self-closing); 54/54 tests. Implementation complete on `feat/intent-agent`, pending review/merge.
- 2026-07-14: Phase 11 follow-up (found in /refine testing) — fixed 3 behavior bugs: turn.ts hardcoded 'medium' defeated right-sizing (now honors assessed risk: trivial→3 required, high→6); a deferral ("no tech stack, you choose") was re-asked every turn (now a strong decision, never re-asked); no terminal handoff ("yes" re-offered forever, now DRAFT→IN_PROGRESS→APPROVED + handoff_complete). 56/56 tests.
- 2026-07-14: Phase 11 follow-up UI — PRD/Plan moved to a streaming slide-over drawer (was burying the working memory in the record column); installed @tailwindcss/typography (prose classes were inert → markdown rendered flat).
- 2026-07-14: FEAT-001 resolved — chat transcript persisted on the Intent header (`transcript` Json); reload restores the full conversation.
- 2026-07-15: Brainstormed **Phase 13 — Studio Experience Redesign** (ask-to-enrich · plain language · live Understanding · always-on Build → OKF files named by outcome · top strip status·mode·cost · personas Quick/Balanced/Deep-dive · real DeepInfra values). ADR-0002 amended with the reversals from live testing; roadmap renumbered (arc → 14–20, V2/Future → 21–22).
- 2026-07-15: Completed **Phase 12 — Working-Memory Build Flow & Cost Engine** on branch (5 groups, 71/71, live-verified: build → materialized + actual cost). Pending review/merge.
- 2026-07-15: Started **Phase 12 — Working-Memory Build Flow & Cost Engine** (on feat/intent-agent): pick → clarify(+verify assumptions) → build gate → build run + actual cost. Specs brainstormed (ADR-0002); roadmap renumbered (V2/Future → rows 20–21).
- 2026-07-15: Phase 13 fix (found in live testing) — BUG-003: the Artifacts "MD" button exported the understanding fields as `intent.md` and was mistaken for the file download; removed it (+ dead `exportMd`) so the only download path is the actual deliverable file, named consistently (`plan.md`, OKF). Renamed the `entities` label "What's involved" → "Key items" for clarity. 73/73, tsc 0.
- 2026-07-15: Phase 13 UX batch (live feedback) — cost shown as `Est. $max` + `Actual $y` (labeled, no cryptic range: ENH-001); OKF front-matter parsed into a clean metadata header in the file viewer (BUG-004); right column split into three cards — Artifacts / Understanding / Context Graph (ENH-002); Build button always visible above the input, becomes "Rebuild" after a build (force-regenerate: BUG-005); `acceptance_criteria` label → "Outcome"; `entities` label → "Entities". Logged FEAT-005 (publish v1/registry) + FEAT-006 (cyclical conversational refine) as deferred to a future phase. 74/74, tsc 0.
- 2026-07-15: Diagnosed a "User"/"U" identity card (stale JWT — self-resolved on refresh; logged BUG-006 with the email-fallback + callback re-sync fix) and removed the role line (`END_USER`) under the name in the global app-shell user card.
- 2026-07-15: BUG-008 (P1) — intermittent "build didn't finish" on production. The build route had no `maxDuration`, so its ~14s LLM call hit Vercel's default serverless timeout. Fixed with `export const maxDuration = 60` on `/api/agent/turn` (needs a plan allowing ≥60s).
- 2026-07-15: **Merged `feat/intent-agent` → `main`** (clean fast-forward, `f8daf55..d8b293e`, 55 commits / 91 files, 0 conflicts) — Phases 12 + 13 now on main. `main` was untouched since the branch point (collaborator work lives on separate branches), so no rewrite/force needed. Pre-merge main backed up at `backup/main-2026-07-15` (`f8daf55`).
- 2026-07-15: BUG-007 (P1) resolved — the build's raw "Unexpected end of JSON input". `HfLLM` capped every call at 900 tokens → the build's JSON truncated mid-body. Fixed: per-call `maxTokens` (build 8000), truncation-tolerant `extractJson` (+ clean marker error), a build retry + empty-build guard, and friendly-only error surfaces in the API + client (raw detail → console). Live-verified on DeepSeek (plan.md, OKF, ~14s). 80/80, tsc 0.
- 2026-07-14: ADR-0001 + configurable cost estimator built — config-as-data (CostModel/Persona/EstimationPrior, DB + seeded default), pure `estimateCost` (config passed in), reference-class output priors, real-tokenizer seam. Then the in-conversation **persona picker** (ADR-0001 amendment): DECIDE gates on `select_persona`, the user picks the mode in chat, and the choice drives refinement rigor + cost (thorough→6 required slots vs fast→3 on the same intent). 67/67 tests.
- 2026-07-14: **ADR-0001** — configurable, deterministic cost estimator. Re-architected the cost machine into three separable layers: config-as-data (`CostModel`/`Persona`/`EstimationPrior` in Postgres, seeded from a versioned default, editable live — provider re-price reflects immediately), a measurement layer (working memory → tokens, pluggable tokenizer), and a pure `estimateCost` (config flows in, never queried). Output uses **reference-class forecasting** (artifact×complexity priors × persona settings), shown as honest bands; removed the max_output cap (was underestimating large tasks). Calibration hook: `/api/expand` logs estimated-vs-actual output tokens (`CostObservation`) so Phase-18 has a corpus. 65/65 tests; 4 Neon tables pushed.
